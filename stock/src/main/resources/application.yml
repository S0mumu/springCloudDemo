server:
  port: 8011
spring:
  application:
    name: stock-server
  cloud:
    nacos:
      serverAddr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - optional:nacos:${spring.application.name}.yaml